/*
var bodyColor = document.querySelector('body.bg-secondary');
var linkColor = document.querySelector('a#interruptor');
linkColor.addEventListener('click', function () {
    var dark = bodyColor.classList.contains('bg-light');
    bodyColor.classList.toggle('bg-light');
    this.innerHTML = dark ? 'night' : 'day';
});
*/
